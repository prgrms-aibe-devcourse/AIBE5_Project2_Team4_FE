# Style Adjustment Skill
1. First, find ALL sources of the target style (component + global CSS files)
2. Show the user what you found before editing
3. Make small incremental changes (max 10-20px adjustments)
4. After editing, run `npx svelte-check` to verify no breakage
